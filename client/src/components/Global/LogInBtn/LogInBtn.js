import React, { Component } from "react";
import "./LogInBtn.css";
class LogInBtn extends Component {
  // create a function to log user into the site here

  render() {
    return (
      <button type="button" className="btn btn-ds-primary btn-lg">
        Log In
      </button>
    );
  }
}

export default LogInBtn;
